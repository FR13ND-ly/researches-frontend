import { LiteratureIssueDirective } from './literature-issue.directive';

describe('LiteratureIssueDirective', () => {
  it('should create an instance', () => {
    const directive = new LiteratureIssueDirective();
    expect(directive).toBeTruthy();
  });
});
