import { ForeignLiteratureIssueDirective } from './foreign-literature-issue.directive';

describe('ForeignLiteratureIssueDirective', () => {
  it('should create an instance', () => {
    const directive = new ForeignLiteratureIssueDirective();
    expect(directive).toBeTruthy();
  });
});
