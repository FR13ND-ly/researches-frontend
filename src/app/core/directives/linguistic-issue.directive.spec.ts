import { LinguisticIssueDirective } from './linguistic-issue.directive';

describe('LinguisticIssueDirective', () => {
  it('should create an instance', () => {
    const directive = new LinguisticIssueDirective();
    expect(directive).toBeTruthy();
  });
});
